/**
 * debug = true 开启数据打印模式
 */
const debug = true

const baseURL = '/api'

const succCode = [10000] // 成功的code
const loginCode = [20000] // 跳登录页的code

export default {
    debug,
    baseURL,
    succCode,
    loginCode
}
